import {NgModule} from '@angular/core';
import {LedgerService} from './ledger.service';

@NgModule({
  imports: [
  ],
  exports: [
  ],
  providers: [
    LedgerService,
  ],
})
export class LedgerModule {
}
