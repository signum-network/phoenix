language: node_js
node_js:
  - 10
before_install:
  - cd lib
install:
  - npm install
  - npm run bootstrap
cache:
  directories:
    - node_modules
script:
  - npm run test:ci

after-success:
  - bash <(curl -s https://codecov.io/bash)
